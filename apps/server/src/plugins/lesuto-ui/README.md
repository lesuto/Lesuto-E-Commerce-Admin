A Universal UI That We Will Use Throughout The Appication

This enables us to have consistency throughout the Admin UI and Reduces code while increasing maintainability 

Watch command to monitor changes while up 
npx tsc -w --project apps/server/src/lesuto-ui/tsconfig.json