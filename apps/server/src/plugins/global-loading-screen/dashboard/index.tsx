import { defineDashboardExtension } from '@vendure/dashboard';

defineDashboardExtension({
    // We define no routes and no menu items.
    // This file acts purely as an entry point to ensure your code is bundled.
});